First Fullstack project! Utilizes Flask as backend.

To implement: progress bar, download playlists in one click

Known issue: haven't figured out a way to host backend yet, so it doesn't work if I close VScode.
